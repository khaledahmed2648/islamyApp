import 'package:flutter/cupertino.dart';

class HadethLogo extends StatelessWidget {
  const HadethLogo({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Image.asset('assets/images/hadeth_logo.png'),
    );
  }
}
