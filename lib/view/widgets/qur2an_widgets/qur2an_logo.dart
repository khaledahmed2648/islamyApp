import 'package:flutter/cupertino.dart';

class Qur2anLogo extends StatelessWidget {
  const Qur2anLogo({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Image.asset('assets/images/quraanlogo.png'),
    );
  }
}
